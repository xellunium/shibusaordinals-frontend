import classes from "./gap.module.css";

const Gap = () => {
    return (
        <section className={classes.Section}>
            <div></div>
            <div></div>
        </section>
    );
};

export default Gap;
